# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# node_modules and dist are ignored (with an exception for the committed
# dist/index.html placeholder) from the repo root .gitignore instead, since
# rust-embed needs web/dist/ to exist for a bare `cargo build` to compile.
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
